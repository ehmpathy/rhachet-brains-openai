# research: internal factory blockers

## test infra

**status**: ✓ sufficient

extant test infra is adequate:
- `npm run test:integration` runs integration tests
- `TEST_ASSETS_DIR` exists for test fixtures
- `genArtifactGitFile` available for test briefs
- `given/when/then/useThen` from test-fns installed

**no new test infra needed.**

---

## feedback loops

**status**: ✓ acceptable

current feedback loop:
1. edit code
2. run `npm run test:integration -- genBrainAtom.integration.test.ts`
3. ~30s per test (api call latency)

**bottleneck**: api call latency (~2-5s per call). unavoidable for integration tests.

**mitigation**: use gpt-4o-mini (fast model) for tests.

---

## access and credentials

**status**: ✓ sufficient

extant credentials adequate:
- `OPENAI_API_KEY` env var used by integration tests
- same key works for tool use (no separate api)
- api key already verified to work

**no new credentials needed.**

---

## infra control

**status**: ✓ sufficient

no infra changes required:
- openai responses api supports tools natively
- no new endpoints to provision
- no database changes
- no ci/cd changes

---

## other blockers

### rhachet types

**status**: ✓ resolved

rhachet 1.37.15 provides:
- `BrainPlugToolDefinition`
- `BrainPlugToolInvocation`
- `BrainPlugToolExecution`
- `genBrainPlugToolDeclaration`
- `asBrainPlugToolDict`
- `AsBrainPromptFor<TPlugs>` conditional type

all needed contracts available.

### openai sdk types

**status**: ⚠️ need verification

need to verify openai npm package exports types for:
- `ResponseInputItem` (function_call_output)
- `Response.output` item types (function_call)
- `tools` parameter for responses.create

**action**: inspect openai package types before implementation.

### test data

**status**: ✓ minimal

simple mock tools sufficient for tests:
```ts
const lookupTool = genBrainPlugToolDeclaration({
  slug: 'test.lookup',
  schema: {
    input: z.object({ key: z.string() }),
    output: z.object({ value: z.string() }),
  },
  execute: async ({ invocation }) => ({
    value: `result:${invocation.input.key}`,
  }),
});
```

no external test data dependencies.

---

## summary

| area | status | action |
|------|--------|--------|
| test infra | ✓ | none |
| feedback loops | ✓ | use fast model |
| credentials | ✓ | none |
| infra control | ✓ | none |
| rhachet types | ✓ | none |
| openai types | ⚠️ | verify before impl |
| test data | ✓ | inline mock tools |

**no blockers prevent start of implementation.**

