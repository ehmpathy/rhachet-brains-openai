# blackbox criteria matrix: brain atom tool use

## matrix.1 = ask output behavior

| ind: tools plugged | ind: brain decision | dep: output | dep: calls |
|--------------------|---------------------|-------------|------------|
| no | n/a | defined | null |
| empty array | n/a | defined | null |
| yes | can answer | defined | null |
| yes | needs tools | null | defined |

**gap check**: all combinations covered ✓

---

## matrix.2 = tool continuation behavior

| ind: prompt type | ind: on.episode | dep: valid | dep: brain action |
|------------------|-----------------|------------|-------------------|
| string | absent | ✓ | initial prompt |
| string | present | ✓ | follow-up question |
| BrainPlugToolExecution[] | absent | ✗ error | n/a |
| BrainPlugToolExecution[] | present | ✓ | tool result continuation |

**gap check**: all combinations covered ✓

---

## matrix.3 = tool execution signals

| ind: signal | dep: output shape | dep: brain receives |
|-------------|-------------------|---------------------|
| 'success' | TOutput | tool result |
| 'error:constraint' | { error: Error } | validation failure info |
| 'error:malfunction' | { error: Error } | system failure info |

**gap check**: all signal types covered ✓

---

## matrix.4 = multi-turn loop termination

| ind: round | ind: brain state | dep: output | dep: calls | dep: loop action |
|------------|------------------|-------------|------------|------------------|
| 1 | needs more data | null | defined | continue |
| 2 | needs more data | null | defined | continue |
| N | can answer | defined | null | terminate |

**gap check**: loop mechanics covered ✓

---

## matrix.5 = multiple tool invocations

| ind: tools count | ind: brain needs | dep: calls.tools.length | dep: execution |
|------------------|------------------|-------------------------|----------------|
| 1 | 1 tool | 1 | sequential ok |
| N | 1 tool | 1 | sequential ok |
| N | M tools | M | parallel ok |

**gap check**: single and multiple invocations covered ✓

---

## matrix.6 = slug validation

| ind: invocation.slug | ind: plugged tools | dep: valid |
|----------------------|--------------------|-----------|
| matches plugged tool | [tool] | ✓ |
| does not match | [tool] | ✗ error |

**gap check**: validation covered ✓

---

## decomposition analysis

**dimension count**: max 2-3 independent variables per matrix

**verdict**: no decomposition needed — matrices are well-scoped

the behavioral boundaries are clean:
- ask output behavior (2 dims)
- continuation behavior (2 dims)
- execution signals (1 dim)
- loop termination (2 dims)
- multiple invocations (2 dims)
- slug validation (2 dims)

each matrix is small and complete.
