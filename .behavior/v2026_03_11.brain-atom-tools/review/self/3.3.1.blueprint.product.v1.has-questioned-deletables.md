# self-review: has-questioned-deletables

## components reviewed

### 1. tool definition conversion

**question**: can this be removed?

**answer**: no. required by openai responses api. without conversion from rhachet format to openai function format, brain cannot receive tools.

**verdict**: retain

---

### 2. prompt type branch

**question**: can this be removed?

**answer**: no. rhachet contract `AsBrainPromptFor<TPlugs>` requires the ability to accept `BrainPlugToolExecution[]` when tools plugged. this is the continuation mechanism.

**verdict**: retain

---

### 3. function_call item extraction

**question**: can this be removed?

**answer**: no. required to parse tool invocations from response.output. without this, we cannot return `calls.tools`.

**verdict**: retain

---

### 4. response_format mutual exclusivity

**question**: can this be removed?

**answer**: no. openai docs state response_format and tools serve different purposes. research confirmed they are mutually exclusive. required for correct api usage.

**verdict**: retain

---

### 5. test cases

**question**: can any test case be removed?

| test case | required? | justification |
|-----------|-----------|---------------|
| tool invocation flow | yes | core usecase from blackbox criteria |
| tool continuation | yes | core usecase from blackbox criteria |
| direct output with tools | yes | matrix.1 row 3: brain can answer |
| no tools plugged | yes | matrix.1 rows 1-2: extant behavior |
| multiple tool invocations | yes | matrix.5: multiple invocations |
| error signal flow | yes | matrix.3: signal types |

**verdict**: all required by blackbox criteria matrices

---

### 6. contracts used table

**question**: can this be removed?

**answer**: yes, but serves as documentation. types are obvious from imports.

**verdict**: retain for clarity

---

### 7. openai map table

**question**: can this be removed?

**answer**: yes, could be inline comments. but serves as reference at implementation time.

**verdict**: retain for clarity

---

### 8. key implementation details

**question**: can this be removed?

**answer**: yes, could defer to implementation phase. but provides concrete examples that clarify the blueprint.

**verdict**: retain for clarity

---

## simplest version that works

the blueprint already represents the minimum required to satisfy the behavior declaration:

- 2 files updated (prod + test)
- each codepath maps to a requirement from vision or criteria
- no helper functions extracted (inline for simplicity)
- no new files created

**no deletions identified.**

