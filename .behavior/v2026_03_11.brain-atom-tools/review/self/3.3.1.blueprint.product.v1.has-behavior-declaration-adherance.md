# self-review: has-behavior-declaration-adherance

## question

does the blueprint adhere to the behavior declarations (wish, vision, criteria)?

---

## wish adherance

| wish requirement | blueprint adherance | verdict |
|------------------|---------------------|---------|
| "support plugs.tools in atoms" | codepath: tool definition conversion, function_call extraction | ✓ adheres |
| "integration and acceptance tests" | 6 test cases defined in test coverage table | ✓ adheres |
| "w/ and w/o output schema" | codepath: response_format conditional based on hasTools | ✓ adheres |

**why wish adherance holds**:

1. **plugs.tools support**: the blueprint defines codepaths to convert `BrainPlugToolDefinition[]` to openai function format and extract `function_call` items from response output. this directly implements the wish.

2. **integration tests**: the test coverage table lists 6 specific test cases that cover tool invocation flow, continuation, direct output, no tools, multiple invocations, and error signals.

3. **w/ and w/o output schema**: the blueprint explicitly handles mutual exclusivity between `response_format` and `tools`. the `hasTools` check gates which mode is used.

---

## vision adherance

### day-in-the-life alignment

| vision scenario | blueprint support | verdict |
|-----------------|-------------------|---------|
| developer plugs tools into atom | codepath: plugs.tools parameter accepted | ✓ adheres |
| brain returns tool invocations | codepath: function_call extraction → BrainPlugToolInvocation[] | ✓ adheres |
| caller executes and continues | codepath: prompt type branch for BrainPlugToolExecution[] | ✓ adheres |
| loop until output defined | test: tool continuation proves two-turn loop | ✓ adheres |

### before/after contrast alignment

| vision before/after | blueprint implementation | verdict |
|---------------------|--------------------------|---------|
| "output always present" → "output nullable when brain defers" | codepath: null output when function_call items present | ✓ adheres |
| "calls always null" → "calls contains tool invocations when plugged" | codepath: BrainOutput construction with calls.tools | ✓ adheres |

### code example alignment

| vision code pattern | blueprint support | verdict |
|---------------------|-------------------|---------|
| `plugs: { tools }` parameter | codepath: plugs.tools accepted in ask() | ✓ adheres |
| `result.calls.tools` output | codepath: BrainPlugToolInvocation[] extraction | ✓ adheres |
| `BrainPlugToolExecution[]` as prompt | codepath: prompt type branch | ✓ adheres |
| `on: { episode }` for continuation | codepath: retain prior episode exchanges | ✓ adheres |

**why vision adherance holds**:

the blueprint codepaths map 1:1 to the vision scenarios:

1. **"developer plugs tools into atom"**: the codepath tree shows `[+] create: tool definition conversion` which maps `plugs.tools` to openai function format. the ask() method signature update includes TPlugs generic.

2. **"brain returns tool invocations"**: the codepath tree shows `[+] create: function_call item extraction` with explicit mapping to `BrainPlugToolInvocation[]` including exid, slug, and input fields.

3. **"caller executes and continues"**: the codepath tree shows `[+] create: prompt type branch` which detects `BrainPlugToolExecution[]` and maps to `function_call_output` items. this enables continuation.

4. **"loop until output defined"**: the test structure includes "tool continuation" which proves a two-turn loop works (initial prompt → tool invocation → tool result → final output).

5. **before/after contract**: the codepath tree shows `[+] create: null output when function_call items present` and `[~] update: BrainOutput construction` with output/calls fields. this fulfills the vision's "output nullable when brain defers".

---

## criteria adherance

### usecase adherance

| usecase | blueprint adherance | verdict |
|---------|---------------------|---------|
| usecase.1 tool invocation flow | all 5 then clauses have codepath/test coverage | ✓ adheres |
| usecase.2 direct output (no tool use) | test case "direct output with tools" | ✓ adheres |
| usecase.3 no tools plugged | test case "no tools plugged" | ✓ adheres |
| usecase.4 multiple tool invocations | test case "multiple tool invocations" | ✓ adheres |
| usecase.5 multi-turn tool loop | test case "tool continuation" proves loop | ✓ adheres |

### exchange adherance

| exchange | blueprint adherance | verdict |
|----------|---------------------|---------|
| exchange.1 string prompt | codepath: "if prompt is string: compose as user message" | ✓ adheres |
| exchange.2 tool execution results | codepath: "if prompt is BrainPlugToolExecution[]: map to function_call_output" | ✓ adheres |
| exchange.3 tool execution signals | contracts table shows BrainPlugToolExecution with signal discriminant | ✓ adheres |
| exchange.4 output schema without tools | codepath: response_format only when no tools (extant) | ✓ adheres |
| exchange.5 tools without output schema | codepath: tools parameter independent of response_format | ✓ adheres |

### boundary adherance

| boundary | blueprint adherance | verdict |
|----------|---------------------|---------|
| boundary.1 tool slug validation | not covered (caller responsibility) | n/a |
| boundary.2 episode required for continuation | codepath: "episode must be provided via on.episode" | ✓ adheres |
| boundary.3 empty tools array | hasTools check handles empty array | ✓ adheres |

**why criteria adherance holds**:

every criteria clause in scope for the atom has a matched blueprint element:

1. **usecase.1 tool invocation flow**: verified each then clause:
   - "output is null" → codepath: "null output when function_call items present"
   - "calls.tools contains invocations" → codepath: "function_call extraction → BrainPlugToolInvocation[]"
   - "each invocation has exid, slug, input" → openai map table shows exact field mappings
   - "prompt accepts BrainPlugToolExecution[]" → codepath: "prompt type branch"
   - "episode continues" → codepath: "retain prior episode exchanges"

2. **usecase.2-5**: each has a dedicated test case in the test coverage table. the tests cover direct output, no tools, multiple invocations, and multi-turn loops.

3. **exchange.1-5**: the codepath tree has explicit items for string prompt vs tool execution array, and the response_format conditional handles output schema scenarios.

4. **boundary.1 out of scope**: the atom returns what openai provides. if openai hallucinates a tool slug that does not exist, the caller handles this via `asBrainPlugToolDict` lookup. the atom's job is faithful translation, not validation.

---

## divergences found

**none.**

the blueprint adheres to all behavior declarations. no divergences from wish, vision, or criteria were detected.

---

## summary

| declaration | adherance status |
|-------------|------------------|
| wish | full adherance |
| vision | full adherance |
| criteria (usecases) | full adherance |
| criteria (exchanges) | full adherance |
| criteria (boundaries) | full adherance (boundary.1 out of scope) |

**the blueprint adheres to all behavior declarations.**

