# self-review: has-clear-instructions

## verification

### can foreman follow without prior context?

yes. the playtest:
- states prerequisites explicitly (node 18+, pnpm, OPENAI_API_KEY)
- each step is self-contained
- no assumed knowledge of internal code structure
- test command patterns are standard npm/jest patterns

### are commands copy-pasteable?

yes. reviewed each command:

| command | copy-pasteable? |
|---------|-----------------|
| `npm run test:integration -- genBrainAtom.integration.test.ts -t "case5"` | ✓ |
| `npm run test:integration -- genBrainAtom.integration.test.ts -t "case5.*t1"` | ✓ |
| `npm run test:integration -- genBrainAtom.integration.test.ts -t "case6"` | ✓ |
| `npm run test:integration -- genBrainAtom.integration.test.ts -t "case7"` | ✓ |
| `npm run test:integration -- genBrainAtom.integration.test.ts -t "case8"` | ✓ |
| `npm run test` | ✓ |
| `npm run test:unit -- castIntoOpenaiFunctionTool` | ✓ |

all commands use standard syntax, no shell escapes needed, no special characters.

### are expected outcomes explicit?

yes. each step has:
- **expected outcome** section with specific observable result
- pass/fail criteria section with explicit success indicators

examples:
- "output shows brain returned `output: null` and `calls.tools` with invocations"
- "all 99 tests pass"
- "types, lint, unit, integration, acceptance all green"

## conclusion

- [x] foreman can follow without prior context
- [x] commands are copy-pasteable
- [x] expected outcomes are explicit

the playtest instructions are clear and followable.
